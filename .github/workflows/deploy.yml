name: GitHub Pages désactivé

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: GitHub Pages désactivé
        run: echo "GitHub Pages n'est plus utilisé. Le site est déployé sur la VM Nginx via deploy-proxmox-vm.yml."
